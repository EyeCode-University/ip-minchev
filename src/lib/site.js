// ===== Канонические данные сайта =====
// Единственный источник правды для metadataBase, canonical, robots.txt,
// sitemap.xml и (позже) JSON-LD. Меняется здесь — расходится везде.

// Адрес прода. Вынесен в NEXT_PUBLIC_*, чтобы одна и та же сборка могла
// подниматься на staging-домене без правки кода: значение инлайнится
// на этапе сборки. Хвостовой слэш срезаем — иначе в конкатенациях
// вроде `${SITE_URL}/sitemap.xml` получится двойной слэш.
export const SITE_URL = (
  process.env.NEXT_PUBLIC_SITE_URL || 'https://gidromashprom.ru'
).replace(/\/+$/, '');

// Бренд — то, что видит посетитель и по чему ищут в поиске. Он же выступает
// наименованием правообладателя в метаданных и Schema.org: персональные данные
// владельца на публичных страницах намеренно не раскрываются и остаются только
// в правовых документах (см. OPERATOR в src/lib/privacy.js), где их требует
// Федеральный закон № 152-ФЗ.
export const SITE_NAME = 'gidromashprom';

export const SITE_TITLE = 'gidromashprom — производство промышленного оборудования';

export const SITE_DESCRIPTION =
  'Изготовление гидравлических цилиндров Ø50–800, арматуры высокого давления Ру250, ' +
  'запчастей к насосному оборудованию. Токарная и фрезерная обработка, ЧПУ, ' +
  'закалка ТВЧ. Поставки по всей России.';
